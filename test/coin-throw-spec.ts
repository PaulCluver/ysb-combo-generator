// tslint:disable-next-line:no-implicit-dependencies
import * as chai from "chai";
import { CoinThrow } from "../src/coin-throw";
const expect = chai.expect;

describe("CoinThrow", () => {
  it("should return the expected gua", () => {
    const coinThrow = new CoinThrow();
  });
});
